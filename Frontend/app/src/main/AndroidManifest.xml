<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZamgavocaFront">

        <!-- 런처: SplashActivity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ZamgavocaFront.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.ZamgavocaFront.NoActionBar" />

        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.ZamgavocaFront.NoActionBar" />

        <!-- 구 MainActivity: 오버레이 테스트용 (설정 화면에서 접근 가능) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="오버레이 테스트" />

        <activity
            android:name=".TodayWordActivity"
            android:exported="false"
            android:label="오늘의 단어" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="설정" />

        <activity
            android:name=".MorningTimeSettingActivity"
            android:exported="false"
            android:label="아침 오버레이 시각 설정" />

        <activity
            android:name=".DndAppsSettingActivity"
            android:exported="false"
            android:label="방해금지 앱 설정" />

        <activity
            android:name=".NudgeIntervalSettingActivity"
            android:exported="false"
            android:label="넛지 출현 주기 설정" />

        <activity
            android:name=".pvp.PvpActivity"
            android:exported="false"
            android:label="PVP 배틀" />

        <activity
            android:name=".pvp.PvpQuestionActivity"
            android:exported="false"
            android:label="PVP 문제" />

        <activity
            android:name=".pvp.CollectedCardsActivity"
            android:exported="false"
            android:label="수집된 카드" />

        <service
            android:name=".service.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Vocabulary learning overlay widgets for language study" />
        </service>

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.example.zamgavocafront.MORNING_ALARM" />
                <action android:name="com.example.zamgavocafront.NUDGE_ALARM" />
            </intent-filter>
        </receiver>

    </application>

</manifest>